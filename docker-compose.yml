version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: cradlecare-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cradlecare
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: cradlecare-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  postgres_data:

